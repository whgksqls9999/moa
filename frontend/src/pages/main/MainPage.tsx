/**
 * MainPage 구성
 * - 최상단: 목표 금액 대비 현재 저축액을 좌우를 가로지르는 막대 그래프로 표현 (게이지)
 * 	- 하나의 큰 목표에 포함된 하위 목표들이 차지하고 있는 영역은 분할해서 표시
 *
 *
 * - 최하단: 네비게이션바 -> 로그인 화면을 제외한 모든 화면에서 공통적으로 출력 (DefaultLayout)
 */

export function MainPage() {
	return <div></div>;
}
